export enum UserRole {
  USER = 'user',
  VENDOR = 'vendor',
  ADMIN = 'admin',
}

export enum VendorStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  SUSPENDED = 'suspended',
}

export enum UserStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
}

export enum BannerPosition {
  HERO_BACKGROUND = 'hero_background',
  ABOVE_NAVBAR = 'above_navbar',
  BELOW_HERO = 'below_hero',
  BELOW_BEST_RATE = 'below_best_rate',
  BELOW_BANK_INTEREST = 'below_bank_interest',
  BELOW_STATISTICS = 'below_statistics',
}
